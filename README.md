# This is a mini Twitch-clone(static HTML) that I made for fun.

### It represents a single page stream which is set server-side with semi-anonymous chat

###TODO: add auth, ability to change stream/channel
